# Variáveis de produção do frontend (Next.js)
# No Coolify: configure em Environment Variables do serviço views.
# Para build local: cp .env.production.example .env.production

# URL base da API — só o domínio, SEM "Value", SEM /auth, SEM aspas
API_INTERNAL_URL=https://api.giftmed.org
