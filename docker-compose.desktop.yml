# Override para Docker Desktop (Windows 11, macOS) e WSL2 com Docker Desktop.
# Rede bridge: API publicada na porta do host; o Next resolve o serviço pelo nome "api".
services:
  api:
    ports:
      - "3333:3333"

  web:
    environment:
      API_INTERNAL_URL: http://api:3333
